services:
  - type: web
    name: axiomgfx-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: CORS_ORIGIN
        value: https://axiomgfx-web.onrender.com

  - type: web
    name: axiomgfx-web
    env: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NEXT_PUBLIC_API_BASE
        value: https://axiomgfx-api.onrender.com

